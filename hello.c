#include <stdio.h>
int main() {
    printf("Hello, World!\n");
    return 0;
}

// If you are using VS Code, open the Command Palette (Ctrl+Shift+P), type "C/C++: Edit Configurations (UI)", and add the path to your compiler's include directory (e.g., C:\Program Files\mingw-w64\include) to "Include path".